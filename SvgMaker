import java.io.PrintWrite;

public class SvgMaker
{
  public int imWidth;
  public int imHeight;
  
  private PrintWriter fp;
  
  public SvgMaker(int imWidth, int imHeight, String fileName) throws FileNotFoundException
  {
   
   //capture incoming member vars
    this.imWidth = imWidth; 
    this.imHeight = imHeight;
    File file = new File(fileName);
   //open file
    fp = new PrintWriter(file);
   
   //write the opening svg tag to the file
   
   
  }
  
  public void drawCircle(Circle circle, Fill fill, Stroke stroke)
  {
  }
  public void drawRectangle(Rectangle r, Fill fill, Stroke stroke)
  {
  }
  public void close()
  {
    //write closing svg tag to file
    
    //close file
  }
  
 
 public void drawCircle(Circle circ, Fill fill, Stroke stroke)
 {
    //   <circle cx="cx" cy="cy" r="r"/>
    //circle draw
    System.out.print("<circle cx='" + circ.cx + "' cy='" + circl.cy + "r='" + circ.r + "'");
    //add stroke
    System.out.print(" stroke='rgb(" + stroke.red + stroke.green + stroke.blue + ")' stroke-width='" + stroke.width + "' stroke-opacity='" + stroke.opacity + "'");
    //add fill
    System.out.print(" fill='rgb(" + fill.red + fill.green + fill.blue + ")' fill-opacity='" + fill.opacity + "'>");
    
  }
 
 
 public class Path
 {
    public int n;
    
    public int[] x;
    public int[] y;
    public Path(int n, int[] x , int[] y)
     {
        this.n = n;
        this.x
 
 
    
 
